name: Build and deploy documentation

on:
  push:
    paths:
      - "docs/**"
      - "mkdocs.yml"
  pull_request:
    paths:
      - "docs/**"

permissions:
  contents: read
  pages: write

jobs:
  build:
    name: Build site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f docs/requirements.txt ]; then pip install -r docs/requirements.txt; else pip install mkdocs mkdocs-material; fi

      - name: Build MkDocs site
        run: mkdocs build --site-dir site

      # Deployment removed: this workflow now builds the site only.
      # To publish, run `mkdocs build` locally and deploy with your preferred method.
      